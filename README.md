# Phone Recommender API (Node.js + Express)

Thêm endpoints:
- POST /auth/register
- POST /auth/login
- POST /user/onboarding (Bearer token)

Products:
- GET /products
- GET /products/:id?k=3

Run:
```bash
npm install
npm run dev
```
